# aws-localstack-lab

Legacy LocalStack tooling remains in this repository's root. The actively maintained Terraform infrastructure as code has been relocated under [`terraform-iac-stack/`](terraform-iac-stack/) so it can be pushed to the `terraform-iac-stack` repository.
